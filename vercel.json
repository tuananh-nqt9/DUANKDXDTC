{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma db push --skip-generate && npm run db:seed && next build",
  "installCommand": "npm install"
}
